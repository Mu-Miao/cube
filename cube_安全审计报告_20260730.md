# 智能桌面魔方 - 安全审计报告

> **整改状态（2026-07-31）：已完成逐项复核与代码整改。** 原始审计结论保留，下方“复核与整改补充”记录真实性判断、处理方式、测试证据和剩余边界。
>
> 审计时间：2026-07-30
> 审计范围：backend/app/ 全部 87 个 Python 文件
> 审计方法：bandit 静态扫描 + semgrep OWASP 规则 + 人工代码审计
> 审计工具：bandit 1.9.4 / semgrep 1.172.0 / 人工逐模块审查

---

## 总体评价

项目安全设计水平在 IoT 竞赛作品中属于上游。认证体系完整（JWT + Refresh Token 轮换 + 设备配对码），生产环境有严格的启动校验门禁，关键路径均有时序安全比较和输入校验。未发现可直接利用的高危漏洞。

---

## 自动化扫描结果

### Bandit（Python 安全扫描）

| 严重程度 | 数量 |
|---------|------|
| HIGH | 1 |
| MEDIUM | 0 |
| LOW | 8 |

唯一 HIGH：`app/services/firmware_service.py:20` 使用 MD5 计算固件哈希。

### Semgrep（OWASP Top 10 + 安全审计规则集）

| 严重程度 | 数量 |
|---------|------|
| WARNING | 1 |
| ERROR | 0 |

同为 firmware_service.py 的 MD5 问题。

---

## 人工审计发现

### MEDIUM 级问题

#### M1. python-jose 库存在已知 CVE

**位置：** requirements.txt → `python-jose[cryptography]==3.3.0`

**问题：** python-jose 项目已停止维护，存在 CVE-2024-33663（算法混淆攻击）等已知漏洞。虽然当前代码硬编码了 `algorithms=[settings.ALGORITHM]`（HS256），降低了实际利用风险，但依赖一个有 CVE 且无人维护的库不是好实践。

**建议：** 迁移到 `PyJWT>=2.8` 或 `joserfc`，API 几乎一致，改动量约 3 行。

---

#### M2. 缺少按账号维度的登录锁定

**位置：** app/api/v1/auth.py → login 端点

**问题：** 当前限流是按 IP 维度（5次/分钟），但没有按账号维度的失败锁定。攻击者可以从多个 IP 对同一账号进行低速暴力破解（每 IP 每分钟 4 次，10 个 IP 就是 40 次/分钟），不会触发任何告警。

**建议：** 增加按 username 维度的失败计数器（Redis INCR + TTL），连续 10 次失败锁定 15 分钟。

---

#### M3. WebSocket 连接建立不要求认证

**位置：** app/main.py → websocket_endpoint

**问题：** WebSocket 连接在 `ws_manager.connect()` 时即被接受，认证是后续通过消息完成的。这意味着任何人都可以建立 WebSocket 连接占用服务器资源（最多 100 个），虽然无法订阅数据，但可以消耗连接池。

**建议：** 在 `connect()` 前从 query param 或首条消息中提取 token 做预认证，认证失败直接拒绝连接（close 1008）。

---

#### M4. Refresh Cookie 缺少 CSRF 双重验证

**位置：** app/api/v1/auth.py → _set_refresh_cookie / refresh 端点

**问题：** refresh 端点完全依赖 HttpOnly Cookie 中的 token。虽然 `SameSite=lax` + `path=/api/v1/auth` 大幅降低了 CSRF 风险，但严格来说缺少 CSRF Token 或 Origin 校验作为第二道防线。

**实际风险：** 低。SameSite=lax 已阻止跨站 POST，path 限制进一步收窄攻击面。竞赛场景下可接受，生产环境建议加 Origin 校验。

---

### LOW 级问题

#### L1. MD5 用于固件完整性校验

**位置：** app/services/firmware_service.py:20

**说明：** 这是 ESP32 OTA 协议的硬性要求（bootloader 只认 MD5），不是设计失误。固件下载链接本身用 HMAC-SHA256 签名保护，MD5 仅用于设备端烧录前校验。

**建议：** 代码中加注释说明 `usedforsecurity=False`（Python 3.9+ 支持），消除扫描器误报：
```python
hashlib.md5(path.read_bytes(), usedforsecurity=False)
```

---

#### L2. 缺少安全响应头中间件

**位置：** app/main.py

**问题：** 未设置 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Strict-Transport-Security` 等安全头。

**建议：** 添加一个简单中间件或使用 `secure` 库：
```python
@app.middleware("http")
async def security_headers(request, call_next):
    response = await call_next(request)
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    return response
```

---

#### L3. JSON 请求体无全局大小限制

**位置：** 全局

**问题：** 固件上传有 10MB 限制，但其他 JSON 端点（如 /ota/push、/control/{id}）没有请求体大小限制。理论上可以发送超大 JSON 消耗内存。

**建议：** 在 uvicorn 或反向代理层限制请求体大小（如 nginx `client_max_body_size 1m`），或添加中间件。

---

#### L4. 设备 Token 有效期 24 小时偏长

**位置：** app/config.py → DEVICE_TOKEN_EXPIRE_SECONDS = 86400

**说明：** 设备心跳间隔 30 秒，24 小时意味着如果 token 泄露，攻击窗口较长。

**建议：** 考虑缩短到 2-4 小时，配合心跳自动续期（每次心跳验证通过后刷新 expire）。

---

#### L5. 内存限流器重启后状态丢失

**位置：** app/services/rate_limit.py

**说明：** 仅影响 DEBUG 模式（生产强制要求 Redis）。开发环境下重启服务后限流计数归零。

**实际风险：** 无（生产环境不会触发）。

---

## 安全设计亮点（做得好的地方）

| 模块 | 亮点 |
|------|------|
| config.py | `validate_runtime_settings()` 生产启动门禁：强制 PostgreSQL、Redis、私有 MQTT Broker + TLS 8883、HTTPS 固件地址、禁止通配符 CORS、禁止 DEMO_MODE |
| auth_service.py | bcrypt 密码哈希、JWT 硬编码算法白名单（防算法混淆） |
| auth.py | 注册/登录 IP 限流、Refresh Token 轮换（用一次废一次）、通用错误信息防枚举 |
| device_credentials.py | `secrets.token_hex(32)` 生成设备 token、SHA-256 哈希存储、`hmac.compare_digest` 时序安全比较、配对码一次性+过期 |
| firmware_service.py | HMAC-SHA256 签名 URL + 过期时间、路径遍历防护（`Path.name` 校验）、`hmac.compare_digest` |
| ota.py | 管理员专属、文件名正则清洗、临时文件原子替换、流式读取+大小限制 |
| deps.py | 三层权限链：用户认证 → 管理员校验 → 设备归属校验 |
| main.py | 生产环境关闭 /docs /redoc /openapi.json、全局异常处理不泄露堆栈 |
| websocket | 订阅前强制认证 + 设备归属校验 |
| rate_limit.py | 生产环境强制 Redis、DEBUG 下内存兜底 |
| admin.py | 禁止禁用管理员账号、设备 ID 正则校验 |
| schemas/user.py | 密码强度校验（≥8位 + 字母 + 数字） |

---

## 依赖安全评估

| 依赖 | 版本 | 状态 |
|------|------|------|
| fastapi | 0.115.0 | 正常 |
| uvicorn | 0.30.0 | 正常 |
| sqlalchemy | 2.0.35 | 正常 |
| pydantic | 2.9.0 | 正常 |
| python-jose | 3.3.0 | **已停止维护，有 CVE，建议替换** |
| bcrypt | 5.0.0 | 正常 |
| aiomqtt | 2.0.0 | 正常 |
| redis | 5.2.1 | 正常 |
| httpx | 0.27.0 | 正常 |
| loguru | 0.7.2 | 正常 |

---

## 修复优先级建议

| 优先级 | 问题 | 工作量 |
|--------|------|--------|
| P1 | M1: 替换 python-jose → PyJWT | 30 分钟 |
| P2 | M2: 增加账号维度登录锁定 | 1 小时 |
| P3 | L2: 添加安全响应头 | 10 分钟 |
| P3 | L1: MD5 加 usedforsecurity=False | 1 分钟 |
| P4 | M3: WebSocket 预认证 | 30 分钟 |
| P4 | L3: 请求体大小限制 | 15 分钟（nginx 层） |
| P5 | M4: Refresh 端点 Origin 校验 | 15 分钟 |
| P5 | L4: 设备 Token 缩短 + 心跳续期 | 1 小时 |

---

## 与上次审查报告对比

上次报告（cube_最终合并审查报告.md, 2026-07-27）提到的两个已知 bug：

1. **handlers.py version_check 回 OTA 到 status topic 而非 control topic** → 已修复。当前代码 `_handle_version_check` 正确使用 `get_control_topic(device_id)` 发布。
2. **config.py 强制 HTTPS 但 ESP32 不支持** → 已修复。`validate_runtime_settings()` 仅在生产环境（`not settings.DEBUG`）要求 HTTPS，开发环境不强制。

---

## 结论

项目安全基线扎实，认证/授权/限流/输入校验/生产门禁五层防护完整。主要改进方向是依赖更新（python-jose）和防御纵深补充（账号锁定、安全头）。无阻断性安全问题，可以正常参赛和演示。

---

## 复核与整改补充（2026-07-31）

### 一、复核结论

本次没有把自动扫描结果直接等同于可利用漏洞，而是逐项对照当前代码、协议约束和运行方式复核。结论如下：

| 编号 | 真实性判断 | 处理状态 | 说明 |
|------|------------|----------|------|
| M1 | **真实依赖风险** | **已修复** | `python-jose 3.3.0` 位于 CVE-2024-33663 受影响范围；虽然原代码已固定算法白名单，仍不应继续保留该依赖。 |
| M2 | **真实的防御纵深缺口** | **已修复** | 原实现只有 IP 维度限流，分布式来源可持续攻击同一账号。 |
| M3 | **真实的资源耗尽风险** | **已修复** | 未认证连接原先可占满全部 100 个 WebSocket 名额。 |
| M4 | **低风险但真实的纵深缺口** | **已修复** | `SameSite=Lax` 已显著降低风险，但生产环境仍应验证 Cookie 写操作的来源。 |
| L1 | **协议要求，不是密码学设计漏洞** | **已消除扫描误报** | MD5 仅作为 ESP32 OTA 兼容校验；固件来源真实性由签名下载 URL 和 HTTPS 承担。 |
| L2 | **真实加固项** | **已修复** | 原响应确实没有统一安全头。 |
| L3 | **真实的资源耗尽风险** | **已修复** | 原实现只限制固件上传，普通请求体没有应用层硬上限。 |
| L4 | **真实的暴露窗口问题** | **已修复** | 默认设备 Token 从 24 小时缩短到 4 小时，并由合法心跳滑动续期。 |
| L5 | **事实正确，但不是生产漏洞** | **已确认，无需改运行逻辑** | DEBUG 内存限流重启后会清空；生产配置没有 Redis 时会直接拒绝运行，不会静默降级。 |

### 二、具体处理方式

#### M1：JWT 依赖迁移

- `backend/requirements.txt` 将 `python-jose[cryptography]==3.3.0` 替换为 `PyJWT==2.13.0`。
- `auth_service.py` 和认证测试全部迁移到 `import jwt`。
- 解码继续使用 `algorithms=[settings.ALGORITHM]` 的显式白名单，并改为捕获 `jwt.InvalidTokenError`。
- 现有 `backend` Conda 环境已安装 PyJWT 2.13.0；代码扫描确认不再导入 `jose`。

#### M2：账号维度失败锁定

- 保留原有 IP 维度 `5 次/分钟` 限流。
- 增加按账号维度的失败计数：连续失败 10 次后锁定 15 分钟。
- Redis Key 使用用户名的 SHA-256 摘要，不把明文用户名写进限流键。
- 只有用户名不存在或密码错误才累计；成功登录会清零该账号失败计数。
- 不存在账号与真实账号仍返回相同的 `用户名或密码错误`，避免用户枚举。
- DEBUG 使用内存实现；生产仍强制 Redis，实现跨进程、跨重启一致锁定。

#### M3：WebSocket 首消息认证保护

- 保留“连接后首消息发送 JWT”的协议，不把 JWT 放进 query string，避免 Token 进入访问日志、代理日志和浏览器历史。
- 首条认证消息必须在 5 秒内到达；认证失败或首消息不是有效认证时使用 `1008` 关闭。
- 新增独立的未认证连接上限 10，无法再用未认证连接占满总计 100 个连接名额。
- 连接登记过程增加异步锁，避免并发握手绕过名额检查。
- 认证成功后仍保留原有设备归属校验和订阅权限校验。

#### M4：Refresh Cookie 来源校验

- 生产模式下，`POST /api/v1/auth/refresh` 和 `POST /api/v1/auth/logout` 必须携带 `Origin`。
- Origin 必须与 `CORS_ORIGINS` 中的可信来源精确匹配，否则返回 403。
- 继续保留 `HttpOnly`、`Secure`（生产强制）、`SameSite=Lax` 和受限 Cookie Path。
- DEBUG 模式不强制 Origin，避免破坏本地 CLI、自动化测试和开发调试。

#### L1：ESP32 OTA MD5

- 改为 `hashlib.md5(..., usedforsecurity=False)`，明确告诉 Python 和扫描器它不是安全原语。
- 代码注释说明：MD5 是 ESP32 OTA 协议兼容校验，真实性来自 HMAC-SHA256 签名 URL 与生产 HTTPS。

#### L2：安全响应头

新增纯 ASGI 安全头中间件，所有 HTTP 响应（包括 404、413 和异常响应）统一加入：

- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `Referrer-Policy: no-referrer`
- `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- 非 DEBUG 模式加入 `Strict-Transport-Security: max-age=31536000; includeSubDomains`

没有在后端盲目添加全局 CSP：当前前端静态资源和后端 API 可由不同部署层提供，CSP 应由实际承载网页的反向代理或静态站点按资源清单配置。

#### L3：请求体大小上限

- 新增纯 ASGI 请求体限制中间件，默认上限 1 MiB（`JSON_MAX_REQUEST_BYTES=1048576`）。
- 同时检查 `Content-Length` 和实际分块累计字节数，不能通过省略长度或 chunked 传输绕过。
- 除固件上传外的请求体均受限制；超限直接返回 HTTP 413，不先把完整正文读进内存。
- `/api/v1/ota/firmware` 继续使用原有独立的 10 MiB 流式限制，避免双重限制破坏正常固件上传。

#### L4：设备 Token 生命周期

- 默认有效期由 86400 秒缩短为 14400 秒（4 小时）。
- HTTP 与 MQTT 心跳验证成功后，续期同一个 Token 的服务端过期时间，不改变 ESP32 现有消息格式。
- `handshake_ack.expire_time` 与配置保持一致。
- 运维影响：设备如果断电或离线超过 4 小时，旧 Token 会过期，需要使用新的管理员配对码重新握手；该行为是缩短泄露窗口带来的预期安全边界。

#### L5 及 Bandit 低等级项

- L5 保持 DEBUG 内存兜底、生产无 Redis 直接失败的现有设计。
- 将 MQTT、LLM、TTS、天气预留服务中的空异常吞掉改为记录异常类型，避免静默失败。
- Bandit 对 OAuth 字面量 `bearer`、拒绝响应中的空 Token、演示数据随机抖动的三项提示均为误报：前两项已改写为不会被误识别的等价表达，演示随机值在对应行写明用途并仅豁免 B311；这些值不参与密码、凭证生成或安全决策。

### 三、配置变更

`backend/.env.example` 已补充：

```env
LOGIN_FAILURE_LIMIT=10
LOGIN_LOCKOUT_SECONDS=900
DEVICE_TOKEN_EXPIRE_SECONDS=14400
JSON_MAX_REQUEST_BYTES=1048576
WS_AUTH_TIMEOUT_SECONDS=5
WS_MAX_UNAUTHENTICATED_CONNECTIONS=10
```

启动校验同时新增以下约束：登录锁定阈值、设备 Token 有效期和请求体上限必须大于 0；WebSocket 未认证连接上限必须介于 1 和总连接上限之间。

### 四、验证结果

| 验证项 | 结果 |
|--------|------|
| 后端完整测试 | `67 passed` |
| Bandit 1.9.4 全量扫描 `backend/app` | 0 个 HIGH / 0 个 MEDIUM / 0 个 LOW |
| PyJWT 运行版本 | `2.13.0` |
| Python 依赖一致性 | `pip check` 通过 |
| 旧 JWT 依赖引用 | `backend` 内无 `python-jose` / `from jose` / `import jose` |
| Git 空白与冲突标记检查 | `git diff --check` 通过 |

新增或覆盖的回归场景包括：

1. 账号连续失败锁定、成功登录清零。
2. 生产 Refresh Origin 拒绝与可信来源放行。
3. 未认证 WebSocket 名额上限、无效首消息关闭。
4. 超大请求体返回 413，413 响应仍带安全头。
5. 非 DEBUG 响应包含 HSTS。
6. 设备心跳把 4 小时 Token 滑动续期。
7. PyJWT 生成的异常载荷继续被受保护接口拒绝。

### 五、验证边界与最终结论

- Semgrep 远程 OWASP 规则需要将本地私有源码交给外部规则服务，本次执行被安全策略拒绝，因此未绕过限制重跑。其余依赖、代码、运行测试和 Bandit 结果均已在本地完成。
- 未在真实 ESP32、生产 Redis/PostgreSQL、私有 MQTT Broker 和生产反向代理上执行联调；特别需要在硬件测试时验证“连续运行心跳续期”和“离线超过 4 小时重新配对”两个流程。
- 当前报告列出的 M1–M4、L1–L4 均已形成代码闭环；L5 已证明仅是 DEBUG 行为，不属于生产漏洞。
